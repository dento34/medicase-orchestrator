# MediCase Orchestrator — Python dependencies for Coded Agents
#
# Pinned to ranges that work with Python 3.11+. We use Python 3.14 locally.

# Core
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
pydantic>=2.6,<3.0

# UiPath
uipath>=1.0,<2.0
uipath-langchain>=0.1,<1.0

# LLM / agents
anthropic>=0.40,<1.0
langchain>=0.3,<1.0
langchain-anthropic>=0.3,<1.0
crewai>=0.80,<1.0

# Audio / voice (LanguageAgent)
# Browser-side handles TTS/STT in v1; these are reserved for server-side fallback.
# openai-whisper>=20240930   # uncomment if we add server-side STT

# Storage
sqlalchemy>=2.0,<3.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
